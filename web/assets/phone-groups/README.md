# Shared phone renders

These grouped assets are used by `web/engine.js` for visually similar Galaxy
families. The Fold asset currently uses the supplied foldable composite render;
it can be replaced with a dedicated Fold-only render later without changing
the model mapping.
