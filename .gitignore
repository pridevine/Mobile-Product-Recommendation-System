# Environment / secrets
.env

# Python
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/
.virtual_documents/

# Conda
.conda/

# Generated by src/data_pipeline.py — regenerate with `python -m src.data_pipeline`
data/cleaned_phone_data.csv
data/normalized_scores.csv

# Local phone product photos (copyrighted — not committed; SVG fallback is used
# when these are absent). Keep the README, ignore the images.
assets/phones/*.png
assets/phones/*.jpg
assets/phones/*.jpeg
assets/phones/*.webp

# OS
.DS_Store
Thumbs.db

# Vercel (added by `vercel link`) — .env.local holds a pulled OIDC token
.vercel
.env*
# ...but .env.example documents what to set, so it stays tracked.
!.env.example
