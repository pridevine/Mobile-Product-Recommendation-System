# Optional cloud LLM provider (not required — the app runs primarily on a
# local Ollama model, and falls back to rule-based templates if neither is
# configured). Uncomment and fill in only if you want to wire a cloud provider
# instead of / alongside the local model.

# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_API_KEY=
